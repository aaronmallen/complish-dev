#!/usr/bin/env sh
#MISE description="Check EditorConfig compliance"
#MISE alias=["l:ec", "l:editorconfig", "lint:ec", "lint:editorconfig"]

if command -v editorconfig-checker >/dev/null 2>&1; then
  editorconfig-checker
fi
